import React from 'react';
import {View, Text, StyleSheet} from 'react-native';


const DeckDetailScreen = (props) => {
    return (
        <View><Text>Deck Details Screen</Text></View>
    );
};

const styles = StyleSheet.create({

});

export default DeckDetailScreen;
